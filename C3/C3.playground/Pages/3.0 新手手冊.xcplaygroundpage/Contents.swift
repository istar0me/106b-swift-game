//: # 讓 Playground 更好看
/*:
在進入課程內容之前，我們先來了解一些基本概念吧～
 
 如果你在第一行看見 `//:` 的符號，就代表你現在正在看原始的格式，這樣雖然方便修改程式碼，但並不好看
 
 因此請大家先在上方的選單尋找「Editor」，並點選最下方的「Show Rendered Markup」，來查看渲染過後的版本
 
 註：若要還原成原本的顯示方式，則變成「Editor」->「Show Raw Markup」
 */
/*:
 # Mac 上鍵盤按鍵
 大家可能是第一次操作 Mac OS，為了讓大家更好上手，特此將常用按鍵的簡稱、全名、符號與 Windows 上相對按鍵標示出來，方便大家查看
 
 (按鍵由上至下、由左至右排序)：
 * Shift(⇧) = (相同)
 * Ctrl/Control(⌃) = (相同)
 * Cmd/Command(⌘) = Windows(視窗)按鍵
 * Option(⌥) = Alt
 > 第一次看到沒記住沒關係，之後可以回來再查看 :)
 */

//: [下一頁](@next)
